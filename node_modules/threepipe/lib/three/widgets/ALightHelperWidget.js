import { AHelperWidget } from './AHelperWidget';
export class ALightHelperWidget extends AHelperWidget {
    constructor(object) {
        super(object);
        this.light = object;
        this.traverse(o => {
            o.userData.__keepShadowDef = true;
            o.castShadow = false;
            o.receiveShadow = false;
        });
    }
}
//# sourceMappingURL=ALightHelperWidget.js.map