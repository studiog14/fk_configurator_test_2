import { AHelperWidget } from './AHelperWidget';
export class ACameraHelperWidget extends AHelperWidget {
    constructor(object) {
        super(object);
        this.camera = object;
        this.traverse(o => {
            o.userData.__keepShadowDef = true;
            o.castShadow = false;
            o.receiveShadow = false;
        });
    }
}
//# sourceMappingURL=ACameraHelperWidget.js.map